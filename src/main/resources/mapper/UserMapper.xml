<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.repository.UserRepository">

    <select id="getUserById" parameterType="Long" resultType="org.example.domain.user.User">
        SELECT id, name, email, picture, role
        FROM USER
        WHERE id = #{id}
    </select>

    <select id="getUserByEmail" parameterType="String" resultType="org.example.domain.user.User">
        SELECT id, name, email, picture, role
        FROM USER
        WHERE email = #{email}
    </select>

    <insert id="create" parameterType="org.example.domain.user.User">
        INSERT INTO USER
            (name, email, picture, role)
        VALUES (#{name}, #{email}, #{picture}, #{role});
    </insert>

    <delete id="delete" parameterType="org.example.domain.user.User">
        DELETE
        FROM USER
        WHERE id = #{id}
    </delete>

</mapper>
